# Work Registry

Recent significant work. Check before starting new tasks.

## Recent

<!-- Format: - YYYY-MM-DD | topic | one-line summary -->
- 2026-07-06 | persist inbound messages + reprocess (TD-009) | `fix/td-004-food-image` (NOT committed yet): **TD-009** — new `inbound_messages` table (model/schema/CRUD/`inbound_message_service.py`/migration `e4f5a6b7c8d9`) persists every meal photo/text on receipt **before** the OpenAI call (`status=pending`), so a failed/dropped inbound no longer vanishes (the deprecated-model outage lost messages: Telegram ack'd, DB empty). `process_meal_input` records best-effort; `_run_meal_analysis` flips `analyzed`(+parsed nutrition)/`failed`(+error), threaded through the TD-005 self-heal retry (`inbound_id` carried in `pending_analysis`). Owner-only `/reprocess` re-analyzes pending/failed rows with the current model (cap `REPROCESS_BATCH_LIMIT=20`, stops early if model still gone) — "after a model fix, replay what failed". Retention: `INBOUND_MESSAGE_RETENTION_DAYS=60` + daily Celery-beat `purge_inbound_messages`. Applied the TD-006 lesson (`created_at` NOT NULL + `server_default`). Scope (owner-picked via AskUserQuestion): **file_id-only** + **`/reprocess` command**; disk-bytes archival + delete-on-request + reprocess→meal deferred to new **TD-010** (Low). Then /review-deep (bug/arch/silent/claudemd) → fixes: reply-failure no longer mislabels a clean analysis as `failed`; shared `_analyze_and_parse` helper (replay routes through the ai_call_logs audit trail, no duplicated pipeline); `mark_*` return bool so /reprocess counts only persisted writes; `exc_info` on best-effort logs. 18 new tests, **109 green**. Extraction into a service keeps `telegram.py` from ballooning further (still see TD-008).
- 2026-07-06 | model self-heal (TD-005/006) + review fixes | `fix/td-004-food-image` (+3 commits, **NOT pushed**): **TD-006** — `ai_call_logs.created_at` made `NOT NULL`+`server_default` (edited the un-deployed migration in place, no follow-up). **TD-005** — self-heal against OpenAI model deprecation: `OpenAIService._create` translates `model_not_found`/deprecation → typed `ModelUnavailableError`; `list_suitable_models()` filters `/v1/models` by a maintained family allowlist (minus non-chat variants) w/ static fallback; new `CHOOSING_MODEL` **ReplyKeyboard** conversation state (chosen over an inline `CallbackQueryHandler` to stay inside the ConversationHandler) lets the owner pick + **auto-retries** the stashed analysis; choice persisted to a new generic `app_settings` KV table (model/crud/migration `d3e4f5a6b7c8`, loaded at startup). Then `/review-deep` (bug/silent-failure/arch/claudemd) → applied fixes: `foods` element str-coercion (latent join crash on swapped-in models), tightened model-error detection, `crud_app_setting.set` rollback, `app_settings.created_at`, `_create` return hint; logged **TD-007** (self-heal doesn't cover the per-request `OpenAIService` in the *unmounted* `ai.py` router) + **TD-008** (extract self-heal into `model_selection.py`). 91 tests green. Aside: accidentally ran `black` project-wide → reverted the ~30 unrelated reformats (keep formatter scoped to touched files).
- 2026-07-06 | food-image + meal-logging robustness | `fix/td-004-food-image` (9 commits, **PUSHED**, not merged): fixed **TD-004** (food-image analysis — deprecated model, nested `image_url`, JSON parse, portion key) + deleted dead `openai.py`; hardened image path (base64 data URL to OpenAI, persist photo `file_id`); DRY parse/reply helpers; OpenAI SDK retries (`OPENAI_MAX_RETRIES`); atomic meal draft + reset on new-entry/reject (fixed a stale-`photos` carryover bug); new **`ai_call_logs`** debug table (model/schema/CRUD/migration `c2d3e4f5a6b7` + `app/services/ai_call_log_service.py`) with `LOG_LEVEL` config and a daily Celery-beat purge (`DEBUG_LOG_RETENTION_DAYS=60`); fixed pre-existing `grant_subscription` session leak + `crud_subscription` silent-commit. Reviewed via /review-deep (4 agents) + /review — clean. 76 tests green. New debt: TD-006 (Low). PR not opened.
- 2026-07-06 | review-note fixes | `feat/unfreeze` (+2 commits, on top of the earlier merge to main): fixed TD-002 (bot token leaked into logs — clamped httpx/httpcore to WARNING in `create_bot_application`) and TD-003 (narrowed `ALLOWED_HOSTS` default `['*']`→loopback); re-ran /review-security + /review-deep (clean; the `docker-compose.prod.yml` "deletion" was a diff artifact — file lives on main, merge preserves it). Logged new debt from first live mini run: TD-004 (Critical — food-image analysis broken, deprecated `gpt-4-vision-preview` hardcoded) + TD-005 (High — self-heal on model_not_found). 45 tests green. NOT pushed.
- 2026-07-02 | bot unfreeze | `feat/unfreeze` (9 commits): committed the `/consult` relay to the my-health hub; fixed schema/test drift (Meal photos/ai_analysis + migration, User.settings, pytz→datetime.UTC); added access-control modes (open/whitelist/closed, default whitelist) with a silent global gate; secured the REST API (`X-API-Token`, fail-closed) + Telegram webhook secret; extracted `app/services/access_control.py`; staged roadmap in `docs/stages/`. Reviewed via /review, /review-deep ×2, /review-security — all findings fixed. Not pushed/merged.

## Archive

Entries older than 3 months moved here.
